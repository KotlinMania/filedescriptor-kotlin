# Port targets — `filedescriptor-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `filedescriptor` that codex-rs uses, and therefore the minimum subset that `filedescriptor-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-52] (rank `150`, tier `direct`, status `To Do`)

**Cargo package name**: `filedescriptor`
**Rust module name (in `use` statements)**: `filedescriptor`

## Summary

- **3** distinct symbols imported by codex-rs
- **5** total `use` statements
- **3** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `filedescriptor::FileDescriptor`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/conpty.rs:23` (`codex-utils-pty`) — `use filedescriptor::FileDescriptor`
- `utils/pty/src/win/psuedocon.rs:27` (`codex-utils-pty`) — `use filedescriptor::FileDescriptor`

### `filedescriptor::OwnedHandle`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/mod.rs:30` (`codex-utils-pty`) — `use filedescriptor::OwnedHandle`
- `utils/pty/src/win/psuedocon.rs:28` (`codex-utils-pty`) — `use filedescriptor::OwnedHandle`

### `filedescriptor::Pipe`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/conpty.rs:24` (`codex-utils-pty`) — `use filedescriptor::Pipe`
