# Rust callers — `filedescriptor-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `filedescriptor` | `filedescriptor` | 19 | 3 | 3 | 5 |

## `filedescriptor`

### Inbound symbols (7 distinct, 19 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `filedescriptor`.

#### `filedescriptor::FileDescriptor` — 6 site(s) (2 codex, 4 other, 2 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:23` — `use filedescriptor::FileDescriptor`
- ✅ `codex-kotlin/codex-utils-pty` `src/win/psuedocon.rs:27` — `use filedescriptor::FileDescriptor`
- `portable-pty-kotlin/portable-pty` `src/serial.rs:13` — `use filedescriptor::FileDescriptor`
- `portable-pty-kotlin/portable-pty` `src/unix.rs:5` — `use filedescriptor::FileDescriptor`
- `portable-pty-kotlin/portable-pty` `src/win/conpty.rs:5` — `use filedescriptor::FileDescriptor`
- `portable-pty-kotlin/portable-pty` `src/win/psuedocon.rs:5` — `use filedescriptor::FileDescriptor`

#### `filedescriptor::OwnedHandle` — 4 site(s) (2 codex, 2 other, 2 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/mod.rs:30` — `use filedescriptor::OwnedHandle`
- ✅ `codex-kotlin/codex-utils-pty` `src/win/psuedocon.rs:28` — `use filedescriptor::OwnedHandle`
- `portable-pty-kotlin/portable-pty` `src/win/mod.rs:17` — `use filedescriptor::OwnedHandle`
- `portable-pty-kotlin/portable-pty` `src/win/psuedocon.rs:5` — `use filedescriptor::OwnedHandle`

#### `filedescriptor::Pipe` — 2 site(s) (1 codex, 1 other, 2 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:24` — `use filedescriptor::Pipe`
- `portable-pty-kotlin/portable-pty` `src/win/conpty.rs:5` — `use filedescriptor::Pipe`

#### `filedescriptor::POLLIN` — 2 site(s) (0 codex, 2 other, 2 caller crates)

- `crossterm-kotlin/crossterm` `src/event/source/unix/tty.rs:12` — `use filedescriptor::POLLIN`
- `portable-pty-kotlin/portable-pty` `src/serial.rs:268` — `use filedescriptor::POLLIN`

#### `filedescriptor::poll` — 2 site(s) (0 codex, 2 other, 2 caller crates)

- `crossterm-kotlin/crossterm` `src/event/source/unix/tty.rs:12` — `use filedescriptor::poll`
- `portable-pty-kotlin/portable-pty` `src/serial.rs:268` — `use filedescriptor::poll`

#### `filedescriptor::pollfd` — 2 site(s) (0 codex, 2 other, 2 caller crates)

- `crossterm-kotlin/crossterm` `src/event/source/unix/tty.rs:12` — `use filedescriptor::pollfd`
- `portable-pty-kotlin/portable-pty` `src/serial.rs:268` — `use filedescriptor::pollfd`

#### `filedescriptor::AsRawSocketDescriptor` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `portable-pty-kotlin/portable-pty` `src/serial.rs:268` — `use filedescriptor::AsRawSocketDescriptor`
