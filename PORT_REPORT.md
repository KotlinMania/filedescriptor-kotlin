# Port report — `filedescriptor-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `filedescriptor`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 19 `use` sites from 3 other repos
- **Outbound**: 58 `use` sites to 3 other repos
- **Unresolved external**: 0 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `filedescriptor` | `filedescriptor` | 19 | 3 | COD-52 | 150 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (19 edges, 3 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `portable-pty-kotlin` | `portable-pty` | 11 |  |
| `codex-kotlin` | `codex-utils-pty` | 5 | ✅ |
| `crossterm-kotlin` | `crossterm` | 3 |  |

### Symbol-level demand (7 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `filedescriptor::FileDescriptor` | 6 | 2 | 4 | 2 |
| `filedescriptor::OwnedHandle` | 4 | 2 | 2 | 2 |
| `filedescriptor::Pipe` | 2 | 1 | 1 | 2 |
| `filedescriptor::POLLIN` | 2 | 0 | 2 | 2 |
| `filedescriptor::poll` | 2 | 0 | 2 | 2 |
| `filedescriptor::pollfd` | 2 | 0 | 2 | 2 |
| `filedescriptor::AsRawSocketDescriptor` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 7 symbols)

##### `filedescriptor::FileDescriptor` (6 sites, 2 codex, 4 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:23` — `use filedescriptor::FileDescriptor`
- `codex-kotlin/codex-utils-pty` `src/win/psuedocon.rs:27` — `use filedescriptor::FileDescriptor`

Other workspace call sites:

- `portable-pty-kotlin/portable-pty` `src/serial.rs:13` — `use filedescriptor::FileDescriptor`
- `portable-pty-kotlin/portable-pty` `src/unix.rs:5` — `use filedescriptor::FileDescriptor`
- `portable-pty-kotlin/portable-pty` `src/win/conpty.rs:5` — `use filedescriptor::FileDescriptor`
- `portable-pty-kotlin/portable-pty` `src/win/psuedocon.rs:5` — `use filedescriptor::FileDescriptor`

##### `filedescriptor::OwnedHandle` (4 sites, 2 codex, 2 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/mod.rs:30` — `use filedescriptor::OwnedHandle`
- `codex-kotlin/codex-utils-pty` `src/win/psuedocon.rs:28` — `use filedescriptor::OwnedHandle`

Other workspace call sites:

- `portable-pty-kotlin/portable-pty` `src/win/mod.rs:17` — `use filedescriptor::OwnedHandle`
- `portable-pty-kotlin/portable-pty` `src/win/psuedocon.rs:5` — `use filedescriptor::OwnedHandle`

##### `filedescriptor::Pipe` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:24` — `use filedescriptor::Pipe`

Other workspace call sites:

- `portable-pty-kotlin/portable-pty` `src/win/conpty.rs:5` — `use filedescriptor::Pipe`

##### `filedescriptor::POLLIN` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/event/source/unix/tty.rs:12` — `use filedescriptor::POLLIN`
- `portable-pty-kotlin/portable-pty` `src/serial.rs:268` — `use filedescriptor::POLLIN`

##### `filedescriptor::poll` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/event/source/unix/tty.rs:12` — `use filedescriptor::poll`
- `portable-pty-kotlin/portable-pty` `src/serial.rs:268` — `use filedescriptor::poll`

##### `filedescriptor::pollfd` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `crossterm-kotlin/crossterm` `src/event/source/unix/tty.rs:12` — `use filedescriptor::pollfd`
- `portable-pty-kotlin/portable-pty` `src/serial.rs:268` — `use filedescriptor::pollfd`

##### `filedescriptor::AsRawSocketDescriptor` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `portable-pty-kotlin/portable-pty` `src/serial.rs:268` — `use filedescriptor::AsRawSocketDescriptor`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `libc-kotlin` | 14 | 11 | `libc` | COD-75 | 3 | To Do |
| `thiserror-kotlin` | 1 | 1 | `thiserror` | COD-153 | 11 | To Do |
| `winapi-kotlin` | 43 | 43 | `winapi` | COD-189 | 128 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

_None — every external dep has a `*-kotlin/` port._
